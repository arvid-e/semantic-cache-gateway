// Placeholder proving the UNIT suite is wired (task 1.2).
// Replace with the first real co-located unit test (task 2.1: config.test.ts).

describe('unit harness', () => {
  it('runs the unit suite', () => {
    expect(true).toBe(true);
  });
});
